#{decorate path=shared.gtmpl/}

<% header.each { fragment -> %>
<div>
    <div>${fragment.content}</div>
</div>
<% } %>

${body}

<% footer.each { fragment -> %>
<div>
    <div>${fragment.content}</div>
</div>
<% } %>
