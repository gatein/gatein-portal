<div class="row-fluid">
  <div id="1" class="span12 sortable <%=l1==null || l1.isEmpty() ? 'emptyContainer' : '' %>" >
    <% l1.each { name, fragment -> %>
    #{window name="${name}" title="${fragment.title}" content="${fragment.content}" /}
    <% } %>
   </div>
</div>
